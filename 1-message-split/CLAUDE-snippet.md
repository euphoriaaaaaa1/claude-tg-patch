<!-- MESSAGE-SPLIT-START · 由 install.sh 注入；删除整段即移除规则 -->

## 消息分段

回复以空行（`\n\n`）分段，每段不超过 30 字，单次最多 3 至 5 段，使用一次 `reply` 调用发送。每段会被 server 拆分为独立的 Telegram 消息逐条发出，段间显示"对方正在输入..."。

不要为了凑分段刻意切句子；长段并入一段、自然短句各占一段。

<!-- MESSAGE-SPLIT-END -->
